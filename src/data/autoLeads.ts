// AUTO-GENERATED — written by odin-pipeline/compose-auto-leads.js. Do not hand-edit.
// Each entry was mapped by the automated build stage (Claude copy-mapping under
// FILL-RULES.md): REWRITEABLE copy traces only to the lead's own scraped site,
// HARD-CLAIM slots are forced to bracket placeholders in code after generation.
// Source of truth: odin-pipeline/outputs/auto-leads/<slug>.json (one file per lead).
import type { Lead } from "./site.ts";
import { SHARED_ABOUT, SHARED_CONTACT, SHARED_CTA } from "./shared.ts";

export const autoLeads: Lead[] = [
];
